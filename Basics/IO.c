#include <stdio.h>
#include <conio.h>

void main()
{
    int a,b,c;
    
    printf("Enter the value of two numbers ");
    scanf("%d %d", &a,&b);
    c=a+b;
    printf("The sum of two number is %d",&c);
    getch();
}
